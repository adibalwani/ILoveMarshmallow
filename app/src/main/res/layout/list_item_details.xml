<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/brand_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="adidas Originals Kids"
        android:textSize="12sp"
        android:textColor="@color/list_item_brand_name"
        style="@style/ListItemDetails"/>

    <TextView
        android:id="@+id/product_name"
        android:layout_below="@id/brand_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Adidas Loop Runner (Toddler)"
        android:textSize="10sp"
        android:textColor="@color/list_item_product_name"
        style="@style/ListItemDetails"/>

    <TextView
        android:id="@+id/price"
        android:layout_below="@id/product_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="$40.00"
        android:textSize="10sp"
        android:textColor="@color/list_item_price"
        android:paddingBottom="@dimen/item_details_padding_bottom"
        style="@style/ListItemDetails"/>

    <RatingBar
        android:id="@+id/product_rating"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:rating="3.0"
        android:numStars="5"
        android:stepSize="1.0"
        android:paddingBottom="@dimen/item_details_padding_bottom"
        style="@style/android:Widget.Material.RatingBar.Small"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginRight="@dimen/item_details_padding_right"/>

</RelativeLayout>